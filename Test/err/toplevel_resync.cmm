int a = 1;
int b = 2
// 顶层（ExtDef 层）的 `ExtDef : error SEMI` 出口：第 1 行出错后在本行的 ';' 处就复原，
// 于是第 2 行的后续语法错误仍报得出来。修复前顶层没有出口，Bison 直接弹空栈中止，
// 这份输入**只报第 1 行**（删掉该产生式本用例立刻变红）。见设计文档 §6.3 / §9.1。
// 说明放在文件**末尾**：这样上面 2 行的行号不随注释行数变化，改注释不会让 .exp 失效。
