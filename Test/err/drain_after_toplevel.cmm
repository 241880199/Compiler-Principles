int a = 1
~
@
// 本用例钉住的：顶层语法错误之后，文件剩余部分的**词法**错误一条都不能少。
// 这里出错点之后**一个 ';' 都没有**，`ExtDef : error SEMI` 永远等不到同步符、
// 无法归约完成，于是 Bison 丢弃到 EOF 后仍然**中止**分析（实测 yyparse() 返回 1）；
// 若错误点之后有 `;`，恢复就能完成、分析会正常走完（见 toplevel_resync.cmm）。
// 说明放在文件**末尾**：这样上面 3 行的行号不随注释行数变化，改注释不会让 .exp 失效。
